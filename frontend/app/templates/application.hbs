<h1>Welcome to the Toys Store</h1>

API Key: {{config.apiKey}} <br />
Authorization Code: {{session.data.authenticated.authorizationCode}} <br />

{{#if session.isAuthenticated}}
  <button onclick={{action "logout"}}>Log Out</button>
{{else}}
    Unauthenticated
{{/if}}

<hr />

{{outlet}}